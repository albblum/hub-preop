# Ambiente Node (opcional). Em deploy real com HTTPS, use production para HSTS nos headers.
# NODE_ENV="development"

# PostgreSQL (see docker-compose.yml in this folder)
DATABASE_URL="postgresql://hub:hub@localhost:5432/hub_preop"

# Auth.js / NextAuth — generate with: openssl rand -base64 32
# Rotating AUTH_SECRET invalidates existing sessions (users must sign in again).
AUTH_SECRET="replace-with-long-random-string"

# Optional: public URL for OAuth callbacks in non-local deploys
# AUTH_URL="http://localhost:3000"

# Optional OAuth (GitHub). Leave unset for Credentials-only sign-in.
# If set, GitHub appears as a provider; use strong secrets and never commit real values.
# AUTH_GITHUB_ID=""
# AUTH_GITHUB_SECRET=""

# Optional: public marketing site (v0 landing). When set, /login?from=public-site shows “Voltar ao site público”.
# NEXT_PUBLIC_LANDING_ORIGIN="https://v0-idr-landing-page.vercel.app"

# CORS: origins allowed from the DocHub public landing (comma-separated, no trailing slashes).
# Applies to POST /api/public/subscribers and POST /api/auth/landing-login (member sign-in from the landing).
# Local: run landing on another port (e.g. 3001) and list http://localhost:3001 here while Hub stays on 3000.
# Example: PUBLIC_SUBSCRIBER_ALLOWED_ORIGINS=https://v0-idr-landing-page.vercel.app,http://localhost:3001
# PUBLIC_SUBSCRIBER_ALLOWED_ORIGINS=""

# ADR 0009 — enable POST /api/instruments/[id]/transition-to-multipart and npm run transition:monolith-to-multipart (admin/registrar only). Omit or 0 = disabled.
# TRANSITION_MONOLITH_TO_MULTIPART_ENABLED="1"

# Seed passwords (founding dev users — change in real environments)
# After changing these, existing DB users keep old hashes unless you run:
# SEED_UPDATE_EXISTING_PASSWORDS=1 npm run seed:users-only
SEED_ADMIN_PASSWORD="ChangeMeAdmin!"
SEED_REVIEWER_PASSWORD="ChangeMeReviewer!"
SEED_VIEWER_PASSWORD="ChangeMeViewer!"
