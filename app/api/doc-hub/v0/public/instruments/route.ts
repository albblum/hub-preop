/**
 * DocHUB-shaped read mirror of the public publication API (ADR 0006).
 * Same contract as GET /api/public/instruments — anonymous, no extra RBAC.
 */
export { GET } from "../../../../public/instruments/route";
